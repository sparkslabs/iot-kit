Make.bat
========

* Uses PY2EXE (you'll need to install Python 2.7 and compatible PY2EXE
  separately) to wrap iotoy_editor.py as a windows .exe

* Creates a distribution in the /dist directory

* Copies in additional dependencies for dfu-programmer.exe

